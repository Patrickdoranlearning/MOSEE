# MOSEE Status

## RESUME POINT

- **Current state**: `209c1f6` depth-driven history chain (US fundamentals ~5yr→20yr via un-shadowed SEC EDGAR, retries, currency-stitch guard, confidence depth honesty — quant-approved) + `c6ce554` fuzzy ticker search & shared Python interpreter (moseePython helper; fixed "Could not find stock data") + `249579e` keyless FX + `05f2c1c` name→ticker search. Also applied the missing `implied_annual_return` DB migration via `init_database()` (on-demand saves were silently failing). Deep dive report/plan + Doubler screener WIP still uncommitted. Nothing pushed. Patrick approved FMP Starter (~$25/mo) for non-US depth — workflow env wiring added (uncommitted).
- **What's next**: Patrick makes decisions D1–D4 in the plan — **D1 first: rotate the leaked freecurrencyapi key (hardcoded in public repo at `scripts/run_mosee.py:190`) and revoke the two GitHub PATs in `PAT.txt` (+ `.ipynb_checkpoints/PAT-checkpoint.txt`)**. Then `jimmy execute plans/PLAN-world-class-mosee.md` Phase 0.
- **Blockers / known state**: (1) Monthly deep-dive on origin/main is broken-but-green: committed code imports `compute_implied_annual_return` which exists only in uncommitted WIP — every ticker ImportErrors, run saves 0 rows, reports success. (2) D2 DECIDED: revert conservative leg to `min()` (plan task 0.4) before committing WIP; the range-position verdict model (winner = price below credible floor) is plan task 3.8. (3) `analyze`/`ai-analysis` API routes unauthenticated + open signup — D3 DECIDED: invite code (task 1.3). (4) Still no `tests/`; 30 eslint errors (29 pre-existing + 1 new accepted `set-state-in-effect` in CommandPalette.tsx:81, consistent with the file's 2 existing instances); Vercel deep-dive env vars still unset. (5) D1 still pending: rotate freecurrencyapi key + revoke the 2 PATs in `PAT.txt` — the key file is deleted from HEAD (`249579e`) but lives in git history. (6) D4 pending: Patrick to dictate his investment rules for `RULES.md`.
- **Standing rules**: Never push without explicit ask. Schema changes land in both `db_client.py` and `db.ts` or not at all. Methodology changes need quant-reviewer before merge. `.gitignore` `/lib/` stays root-anchored (see FIXED-REGISTRY). New: weekly runs are immutable predictions — v1-baseline strategy keeps logging until the 12-month IC report exists (plan §5.1).
- **Intentional leftovers**: Lint debt untouched (tracked on ROADMAP). Doubler screener WIP left uncommitted on purpose (contains the contested methodology change — plan task 0.4/0.5 gates it). Wealth v1 simplifications: per-cure null-reason hints deferred (generic "Add data to track this cure" for all nulls); unauthenticated API calls 307-redirect to /login instead of literal 401 (pre-existing app-wide middleware, security intact); the 8 seeded books were inferred from MOSEE's lenses — Patrick to confirm/extend his actual reading list (content-only edit in `wealth-education.ts`).

---

## Session Handoffs

*(Wrap-up Pillar 4 appends detailed session summaries here, newest on top. The RESUME POINT block above is rewritten each session — it is the 5-bullet TL;DR the next session reads first.)*

### 2026-06-12 — Fuzzy search, interpreter fix, DB migration, history depth (`c6ce554`, `209c1f6`)
- Fuzzy search: `enableFuzzyQuery` on the Yahoo endpoint — "evenplay" → EVPL.L (verified; zero results without it).
- "Could not find stock data" root-caused: preview/ai-analysis/knowledge-base spawned bare `python` (dev shell's .venv lacks yfinance). New `web/src/lib/python.ts` `moseePython()` (MOSEE_PYTHON → venv311 → python) used by all 4 spawn routes.
- "Analysis complete but not loading" root-caused: DB missing `implied_annual_return` column — migration ships in `db_client.init_database()` but never ran (weekly run broken). Applied idempotent init; EVPL.L analysis saved (verdict REDUCE / C / HIGH). Reminder: on-demand path still writes NULL implied_annual_return (plan task 2.1).
- History depth (Patrick: "4 years is not good enough"): scout found the extended-history fetch is a depth no-op AND its success gating skipped SEC EDGAR (20yr US). Fixed: depth-driven source chain (stack sources while <10yr), 2 retries on timeouts, never-cache-failures, confidence depth = max of three statements (+ per-statement detail keys; `cash_flow_years` string key preserved — see FIXED-REGISTRY), FMP currency-stitch guard. Verified live: KO 5yr→20yr; EVPL.L clean at 5yr with honest log. Verifier GREEN; quant APPROVED (2 conditions closed).
- Decisions: FMP Starter approved (~$25/mo) for non-US depth — Patrick to sign up; key goes in `web/.env.local` (`FMP_API_KEY=`) + GitHub secret `FMP_API_KEY` (workflow wiring added, uncommitted). FIXED-REGISTRY +3 entries (depth chain, currency guard, confidence depth). Commits approved; not pushed.

### 2026-06-12 — Ticker name search + keyless FX shipped (`05f2c1c`, `249579e`)
- Patrick's asks: "search everplay → not found" and "build currency exchange without an API". Pipeline: context-scout → 2 parallel feature-builders (both died on ECONNRESET mid-task; completion builder found and fixed a NameError landmine the dropped FX builder left — undefined helpers called in `get_exchange_rate_to_usd`) → verifier GREEN → quant-reviewer APPROVED (2 conditions closed: None guards in convert helpers; ZAc cents divisor live-validated vs NPN/AGL/SOL.JO).
- Search: `/api/ticker-search` (keyless Yahoo search endpoint, server-side fetch, Vercel-safe) + debounced "Market search" section in CommandPalette; "everplay" resolves to EVPL.L and flows into the existing suffix-safe analyze path.
- FX: yfinance `{CCY}USD=X` is now the only FX source — rate-limited, 6h TTL, None-on-failure end-to-end (on-demand path previously crashed on None). `run_mosee.py` (leaked key) and legacy `convert_currency` deleted; forex-python dropped from requirements/setup.
- FIXED-REGISTRY: +1 entry (FX never-fabricate contract), Patrick-approved. Lint debt 29 → 30 (one accepted set-state-in-effect, noted on ROADMAP). Commits approved by Patrick; not pushed.
- Decisions this session: D2 = revert conservative leg to `min()` now, range-position verdict model later (plan task 3.8, winner = price below credible floor); D3 = invite code.

### 2026-06-12 — Wealth education layer shipped (`0267b6f`)
- Full Jimmy pipeline on Patrick's ask "plan, execute and test the entire wealth section… applying education from core books and the investing gurus": context-scout → planner → Karen (APPROVED) → 3 build phases → verifier (tsc/eslint/build all green, 48/48 pages) → tester (10/11 criteria full live pass; #6 substance-pass — 307 vs 401 is pre-existing middleware) → reviewer (approved, 4 P2s → 3 fixed, 1 accepted) → validator (VALIDATED).
- Shipped: `wealth-education.ts` typed registry (8 books, 48 teachings, 11 topics, quote/paraphrase honesty flags + integrity assertion); TeachingCard (UTC-deterministic, hydration-safe) on all 8 tool pages; Learn Library (8 book pages, `generateStaticParams`, KB bridge link); "What the Gurus Say" on cure pages; Skills page + sidebar + DELETE endpoint; debt-payoff calculator tab (surfaces 400); net-worth auto-snapshot with confirm; cure_scores 5/6/7 → `number | null` with "Not tracked" UI and null-excluded averages.
- Zero schema changes (query-only against existing `wt_*` tables — dual-stack rule untouched); cures 1–4 byte-identical (regression-verified).
- FIXED-REGISTRY: 4 new entries (honest cure scores, UTC dayOfYear, cure-5 clamp, Total Repaid from schedule), all Patrick-approved.
- Not pushed. Overnight session's STATUS/ROADMAP edits + report/plan deliberately left uncommitted (STATUS names exact credential-leak locations — don't bake into history before D1 rotation).

### 2026-06-12 — Overnight 9-agent deep dive (plan & review session, no code changed)
- Ran full audit (quant-reviewer, reviewer, drift-detector) + investment committee (Buffett, Munger, Fisher, Lopez de Prado) + data-source research + UX review in parallel; synthesized to `reports/DEEP-DIVE-2026-06-12.md`.
- Wrote `plans/PLAN-world-class-mosee.md` — Karen BLOCKED rev 1 (evidence-clock contradiction, missing 0.4→0.5 dependency), revisions applied, **APPROVED rev 2, Status: Ready**.
- Headline findings: leaked credentials (public repo), silently-broken monthly run on main, contested conservative-leg methodology change in WIP (BLOCKED), FX bug on non-USD Market MoS, Doubler screen lacks quality/verdict gates (value-trap magnet), zero validation evidence — prospective logging loop prescribed, Finnhub/EDGAR-Form-4/FMP data phasing, weekly-diff-digest UX direction.
- ROADMAP: added P0 remediation row. Nothing committed (Patrick asleep; plan requires his D1–D4 decisions first).

### 2026-06-11 — Jimmy agent system installed + deep-dive work landed
- Built the Jimmy coordinator system (modeled on HortiTrack's): 11 dev agents in `.claude/agents/`, command reference in `.claude/jimmy-commands.md`, state files (this file, PROGRESS.md, ROADMAP.md, plans/, FIXED-REGISTRY.md). Council unchanged — Jimmy bridges to it for methodology judgment.
- First `jimmy wrap up` run verified and landed the pre-existing deep-dive work (`4fe67fd`): compileall ✓, tsc ✓, next build ✓, dual-stack parity ✓, cron/auth gating ✓, dispatch↔workflow input wiring ✓.
- **Pillar 1 catch**: bare `lib/` in .gitignore was silently untracking `web/src/lib/deep-dive.ts`, `auth-db.ts`, `wealth-tree-db.ts` — GitHub copy of the repo couldn't build. Fixed (`d5f7d4a`), registered in FIXED-REGISTRY.
- Registry: 1 entry added (root-anchored /lib/). Commits: `d5f7d4a`, `4fe67fd`, + Jimmy system commit. Pushed to main on Patrick's instruction.
